2147483647 会员身份测试截止时间

```
// function payWall(){  
//     var xhrpw = new XMLHttpRequest();
//     xhrpw.open('get', '/index.php/jsapi/paywall');
//     xhrpw.setRequestHeader('Content-Type', 'application/text');
//     xhrpw.onload = function() {
//         if (xhrpw.status === 200) {
//            isReqSuccess = true;
//             var data = xhrpw.responseText;
//             var dataObj = JSON.parse(data); 
//             if (dataObj.paywall >= 1) {      
//                 updateUnlockClass();
//             }else{
//                 updateLockClass();
//                 // console.log('open lock class');
//             }
//         } else {
//             console.log('fail to request');
//         }
//     };
//     xhrpw.send(null);
// }

// var userId1 = GetCookie('USER_ID') ;
// if (userId1 !== null) {
//     payWall(); 
//     var interval = setInterval(function(){
//         payWall();
//     },5000);
//     setTimeout(function( ) {
//       clearInterval(interval); 
//     }, 10000);  
// }

```
## webapp 订阅写法
```
// iapHTMLCode = '<a'+getBuyCode(productID, productPrice, gUserId, productName, tradeNum)+'><button class="iap-move-left">订阅</button></a><p class="iap-teaser">' + productPrice + '/年' + '</p>';

// products[1].state = '<a'+getBuyCode(products[1].id, productPrice, gUserId, productName, orderNum)+'><button class="iap-move-left">现在升级</button></a>';

 onclick="ftjavacriptapp.payzfb('FT0109831523596303','0.01','9270d53f-ac4b-4184-afae-42cacce12193','标准会员')"
```
```
function getWxBuyCode(productId, productPrice, userId, productName, orderNum){ 
    
// function getBuyCode(productId, productPrice, userId, productName, orderNum) {  
//     if (!!userId){
//         var buyCode = ''; 
//         var priceForAndroid = '';

//         var productIDArr = productId.substring(2,5);
//         var productIdStr = (productIDArr == '100') ? 'ftc_premium' : 'ftc_standard';

//         productId = orderNum;

//         priceForAndroid = '0.01';

//         if(osVersion.indexOf('Android')>=0){
//             try {
//                 buyCode = ' onclick="ftjavacriptapp.payzfb(\''+ productId +'\',\''+ priceForAndroid +'\',\''+ userId +'\',\''+ productName +'\')"';

//                 var url = window.location.hash;
//             } catch (ignore) {
//                 alert('请求失败！');
//             }
//             postPayState(productIdStr, productPrice, userId, orderNum, 'start');
            
//         }
//         return buyCode;
//     } 
// }
```

```
var productObj = {};
$('body').on('click', '.openPayment', function(){
    var userId = gUserId || '';
    var productId = $(this).parent().attr('product-id');
    var productPrice = $(this).parent().attr('product-price');
    var productName = $(this).parent().attr('product-title');
    var memberNum = (productName == '高端会员') ? '100' : '010';
    var orderNum = getOrderNum(memberNum);
    getBuyCode(productId, productPrice, userId, productName, orderNum) ;

    productObj.productId = productId;
    productObj.productPrice = productPrice;
    productObj.productName = productName;
    
});



function iapActionsNew(productID, actionType, expireDate) {    
    var iapButtons='';
    var iapHTMLCode = '';
    var productPrice = '';
    var productName ='';
    var tradeNum = '';

    tradeNum = productID;
    var productIDArr = productID.substring(2,5);
    productID = (productIDArr == '100') ? 'ftc_premium' : 'ftc_standard';
    
    iapButtons = document.getElementById('channelview').querySelectorAll('.iap-button');

    // MARK: - Get product type based on its identifiers
    var productType = '';
    if (/premium$|standard$|trial$/.test(productID)) {
        productType = 'membership';
    }else {
        productType = 'eBook';
    }

    postPayState(productID, productPrice, gUserId, tradeNum, actionType);

    switch (actionType) { 
        case 'success':
            if (productType === 'membership') {
                iapHTMLCode = '<a><button class="iap-move-left">已订阅</button></a><p class="iap-teaser">'+ productPrice + '/年' + '</p>';
            } 
            window.location.reload();
            break;
        case 'fail':
            if (productType === 'membership') {  
                turnonOverlay('iap-hint');
                iapHTMLCode = '<a onclick="getBuyCode(\''+ productID +'\',\''+ productPrice +'\',\''+ gUserId +'\',\''+ productName +'\',\''+ tradeNum +'\')"><button class="iap-move-left">订阅</button></a><p class="iap-teaser">' + productPrice + '/年' + '</p>'; 
            } 
            break;
        default:
    }
    if (iapButtons.length>0){
        for (var i = 0,len=iapButtons.length; i < len; i++) {
            if (productID === iapButtons[i].getAttribute('product-id')) {
                iapButtons[i].innerHTML = iapHTMLCode; 
            }
        }
    }
    actionType = '';
}
```

function vipType(dataObj){
    var search = window.location.search;
    if (dataObj.premium >= 1) {      
        updateUnlockClass();
    }else{
        updateLockClass();
    }
}


    // if (paraArr.includes('premium=0')&&paraArr.includes('standard=1')){


## 订阅页面 回答
```
    <!--<div class="o-qas_feedback">
            <p class="o-qas_body"><span><strong>Did this answer your question?</strong></span><button class="o-buttons o-buttons--small">Yes</button><button class="o-buttons o-buttons--small">No</button><span style="display:none;" class="yes_msg_phone">Thank you for your feedback</span></p>
    </div>-->
```

```
// setTimeout(function(){
//     blockDailyEnglish(url);
// },500);

function blockDailyEnglish(url){
    var content = $('#scrollcontainer').html();
    var isFTCpw = Boolean(Number(getCookie('isFTCw')));
    var content = $('.bottom-part').html();
    if (isFTCpw) {
        $('.prev-next').hide();
        $('#scrollcontainer').html(getpaywallHint(url));
        $('.bottom-part').html('<div channel-type='+url+' style="text-align: center;">成为付费会员，阅读FT独家内容<br>请<a style="color:#26747a" iap-action="membership" class="iap-channel" iap-title="会员">点击此处</a> 。</div>');
        window.gSubscriptionEventLabel = getEventLabelFromUrl(url);
        ga('send','event','Android Privileges', 'Display', window.gSubscriptionEventLabel);
    } else {
        $('.prev-next').show();
        $('#scrollcontainer').html(content);
        $('.bottom-part').html(content);
    }
     
}
```

广告：
http://dualstack.adsame-1421766300.ap-southeast-1.elb.amazonaws.com/s?z=ft&c=100016000421&slot=723659&tr=100016000421_5594447974&_dc=2&_mm=2&_sz=2&_am=2&_ut=visitor&_fallback=0&_refresh=0